# City of San Pedro — Office Directory

The rendered office directory is generated from `executive.json`. It contains only contacts verified against the official City Government directory.

The full directory, source URL, and last-checked date are maintained in that data file. Emergency hotlines and external-agency contacts remain deferred until a current primary source confirms them.
